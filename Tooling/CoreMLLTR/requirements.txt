# Core ML LTR training pipeline dependencies.
# Install into a virtualenv (do not install globally):
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# coremltools requires macOS to save/validate .mlpackage models.
numpy>=1.24
coremltools>=7.0
